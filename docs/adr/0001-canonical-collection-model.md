# ADR 0001: Canonical collection model

Accepted. Every source and destination maps through shared identity and collection models so ingestion shape cannot affect planning. Fingerprints are variant-sensitive and versioned; mutable quantity is separate.

