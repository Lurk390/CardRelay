# ADR 0004: Local persistence

Accepted. SQLite provides transactional local mappings, snapshots, cache, plans, and audit history without a server. Alembic versions schema evolution. Credentials, cookie headers, and tokens are excluded.
