# Security model

Trust external files and responses only after validation. Keep secrets in OS/browser-managed local storage, never logs or snapshots. Raw data retention is disabled by default. Dry-run, explicit writes, ambiguity rejection, completeness checks, and separate destructive flags limit collection-corruption risk.

